import domReady from '@wordpress/dom-ready';

domReady( () => {
	/**
	 * adding or modifying a contribution for a ticker and a year, using the form
	 */
	const forms = document.querySelectorAll( '.ticker-contribution-form' );
} );
